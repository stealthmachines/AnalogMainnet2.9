version: '3.8'

services:
  hdgl-bridge:
    volumes:
      - /dev/i2c-1:/dev/i2c-1
    devices:
      - /dev/i2c-1

# Usage (Linux hosts only):
# docker compose -f docker-compose.yml -f docker-compose.i2c.yml up
# This file is intentionally separate so Windows/macOS users don't get device errors.
